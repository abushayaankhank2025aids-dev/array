#include<stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
        printf("enter element %d",i+1);
        scanf("%d",&arr[i]);
    }
    int prod=1;
    for(i=0;i<n;i++)
    {
        prod=prod*arr[i];
    }
    printf("prod: %d",prod);
}
